.PHONY: serve

# Requires Hugo Extended (SCSS compilation). Install: scoop install hugo-extended
# Pinned in CI to v0.100.0; the local `hugo` should match to avoid theme API drift.
serve:
	hugo server
